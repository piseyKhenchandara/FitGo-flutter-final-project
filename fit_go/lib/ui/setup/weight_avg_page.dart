import 'package:flutter/material.dart';

class WeightAvgPage extends StatelessWidget {
  const WeightAvgPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}